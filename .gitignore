# Claude Code local files
CLAUDE.md
.claude/

# Keep README local for now
README.md

# Per-instance output root: output/<host>/{index,downloads,audit}. All runtime-
# generated and regenerable (the index is a point-in-time cache; downloads + audit
# match logs are produced per run). Never source artifacts.
output/
# Legacy locations (pre-output-folder), kept so older working copies stay ignored:
art-downloads/
.artca-index/
*.jsonl
*.jsonl.migrated

# Local paste-bundle generator + its output. The headless-paste bundle lives ONLY on the
# headless-paste branch; never commit the generator or a generated bundle to dev/main.
make-headless-paste.ps1
headless-paste.txt
tui-paste.txt

# Secrets / local config — never commit tokens or API keys
*.secret
*.token
.env
*.local.ps1
secrets.ps1

# PowerShell transcripts / logs
*.log
PowerShell_transcript.*

# Editor / OS cruft
.vscode/
.idea/
*.swp
*~
Thumbs.db
Desktop.ini
.DS_Store
